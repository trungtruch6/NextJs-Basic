import React, { memo } from "react";

const Footer = () => {
  return (
    <div className="h-full">
      <div className="flex items-center justify-center border-t border-gray-300 h-full shadow-md bg-gray-300">
        Next.JS 14 Basic
      </div>
    </div>
  );
};

export default memo(Footer);
